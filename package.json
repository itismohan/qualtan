{
  "name": "mataf-playwright",
  "version": "1.0.0",
  "description": "AI-driven Playwright TypeScript testing engine",
  "main": "index.js",
  "scripts": {
    "test": "playwright test --config=playwright/playwright.config.ts",
    "test:mocks": "playwright test --config=playwright/playwright.config.ts api-network-stubs",
    "codegen": "playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^26.2.0",
    "typescript": "^5.0.0"
  }
}
